package in.rcard.raise4s

import in.rcard.raise4s.RaiseAnyPredef.succeed
import in.rcard.raise4s.Runtime.toEither
import org.scalatest.flatspec.AnyFlatSpec
import org.scalatest.matchers.should.Matchers

class RuntimeSpec extends AnyFlatSpec with Matchers {

  "Runtime.toEither" should "return a Right if it is not an error " in {
    
  }
}